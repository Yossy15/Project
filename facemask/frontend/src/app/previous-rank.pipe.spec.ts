import { PreviousRankPipe } from './previous-rank.pipe';

describe('PreviousRankPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviousRankPipe();
    expect(pipe).toBeTruthy();
  });
});
